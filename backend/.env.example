# ============================================================
# Configuracion del Backend - Orthodonto
# ============================================================
# Copia este archivo a .env y llena los valores reales:
#   cp .env.example .env
#
# IMPORTANTE: Nunca subas el archivo .env ni credentials.json a Git
# ============================================================

# Puerto del servidor (por defecto 3000)
PORT=3000

# ID de tu Google Sheet (se extrae de la URL del Sheet)
# URL: https://docs.google.com/spreadsheets/d/[ESTE_ES_EL_ID]/edit
SPREADSHEET_ID=TU_SPREADSHEET_ID_AQUI

# Nombre exacto de la pestana/hoja donde van las citas
SHEET_NAME=Citas

# ID del Google Calendar donde crear los eventos
# Usa 'primary' para el calendario principal de la cuenta
# O el email de un calendario especifico
CALENDAR_ID=primary

# ============================================================
# ARCHIVO credentials.json
# ============================================================
# Ademas de este .env, necesitas colocar el archivo credentials.json
# en esta misma carpeta (backend/). Este archivo se descarga de
# Google Cloud Console al crear la key de la Service Account.
#
# Pasos:
# 1. Google Cloud Console > APIs & Services > Credentials
# 2. Clic en tu Service Account > Keys > Add Key > JSON
# 3. Renombra el archivo descargado a credentials.json
# 4. Colocalo en la carpeta backend/
# 5. Comparte tu Google Sheet con el client_email de la SA (como Editor)
# ============================================================
